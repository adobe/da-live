/*
 * Stub for hast-util-from-html - never called in browser (DOMParser is available)
 * Only needed to satisfy module resolution during testing
 */

// eslint-disable-next-line import/prefer-default-export
export function fromHtml() {
  throw new Error('hast-util-from-html stub should not be called in browser environment');
}
